# Production Environment Variables
# This file is tracked by git and used for non-sensitive configuration
# Sensitive values should be set in EAS secrets or CI/CD environment

# App Environment
EXPO_PUBLIC_ENV=production

# API Configuration (sensitive keys should be in EAS secrets)
# The actual API key will be injected from EAS secrets during build
# EXPO_PUBLIC_OPENAI_API_KEY is set via EAS secrets